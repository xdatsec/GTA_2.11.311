//// Created by DevHama on 15.09.2025.//#ifndef LR64_GPS_H#define LR64_GPS_H#include "jni.h"#include "main.h"#include "Vector.h"#include <vector>#include <unordered_map>#include <queue>#define MAX_WAZE_DOTS 1000struct MapNode {    int id;    float x, y, z;    std::vector<int> connections;};struct WazePoint {    float x, y;    bool created;};struct AStarNode {    int nodeId;    float g; // cost from start    float h; // heuristic to goal    float f; // g + h    int parent;    bool operator>(const AStarNode& other) const {        return f > other.f;    }};class GPS {public:    static inline CVector to;    static inline bool enabled = false;    static inline RwTexture* marker = nullptr;    // Map nodes data    static inline std::unordered_map<int, MapNode> mapNodes;    static inline bool nodesLoaded = false;    // Waze system    static inline std::vector<WazePoint> wazePoints;    static inline uint32_t lastUpdateTime = 0;    GPS();    static void DoPathDraw();    static void Set(CVector pos, bool toggle);    static void RenderMarker();    static bool LoadGPSData();private:    static void UpdateWazePath();    static void CreateWazePoints(CVector from, CVector to);    static void DestroyWazePoints();    static void RenderWazeOnRadar();    static float GetDistance2D(float x1, float y1, float x2, float y2);    static float GetDistance3D(float x1, float y1, float z1, float x2, float y2, float z2);    static int GetClosestNode(float x, float y, float z);    static std::vector<int> FindPath(int startNode, int endNode);};#endif //LR64_GPS_H