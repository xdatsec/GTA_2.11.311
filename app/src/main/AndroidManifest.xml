<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.kurdish.roleplay">

    <uses-sdk
        android:minSdkVersion="28"
        android:targetSdkVersion="36" />

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.touchscreen.multitouch"
        android:required="false" />

    <!-- Run foreground service. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Allows the app to synchronize data with other services while its foreground service is running. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Allows the app to receive messages from Google Cloud Messaging -->
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />
    <!-- Allows apps to display notifications on an Android device. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Allows apps to access information about how they were installed onto your device. -->
    <uses-permission android:name="com.google.android.finsky.permission.BIND_GET_INSTALL_REFERRER_SERVICE" />

    <!-- Permissions گرنگ -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>

    <uses-permission-sdk-23
        android:name="android.permission.READ_MEDIA_AUDIO" />
    <!-- Read the contents of your shared storage space -->
    <!-- Storage (API 29 → legacy OK, API 30 → MANAGE_EXTERNAL_STORAGE OK) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:targetApi="30" />

    <!-- Voice permissions -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Bluetooth -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"
        tools:targetApi="31"/>

    <!-- Notifications + Firebase -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"
        tools:targetApi="33" />
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission
    android:name="com.rockstargames.gtasa.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION"
    android:protectionLevel="signature" />
    <!-- OpenGL for SAMP -->
    <uses-feature android:glEsVersion="0x00020000" android:required="true"/>

    <application
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:isGame="true"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:hardwareAccelerated="true"
        android:appComponentFactory="androidx.core.app.CoreComponentFactory"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <meta-data
            android:name="firebase_sessions_enabled"
            android:value="false" />

        <!-- FileProvider for update/install -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>


        <!-- Update Service -->
        <service
            android:name=".launcher.UpdateService"
            android:enabled="true"
            android:exported="false" />

        <activity
            android:theme="@style/GameActivity"
            android:name="com.rockstargames.gtasa.DownloaderActivity"
            android:exported="true"
            android:screenOrientation="userLandscape"
            android:resizeableActivity="false">
        </activity>

        <activity
            android:theme="@style/GameActivity"
            android:name="com.rockstargames.gtasa.GameActivity"
            android:launchMode="singleTask"
            android:screenOrientation="userLandscape"
            android:configChanges="fontScale|layoutDirection|smallestScreenSize|screenSize|uiMode|screenLayout|orientation|keyboardHidden|keyboard|locale"
            android:documentLaunchMode="never"
            android:exported="true"
            android:resizeableActivity="false" />
        <!-- Launcher Activity -->
        <activity
            android:name=".launcher.SplashActivity"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|uiMode"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Update Activity -->
        <activity
            android:name=".launcher.UpdateActivity"
            android:screenOrientation="landscape"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|uiMode"
            android:windowSoftInputMode="adjustPan"
            android:exported="true"/>

        <!-- Main UI -->
        <activity
            android:name=".launcher.MainActivity"
            android:screenOrientation="landscape"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|uiMode"
            android:windowSoftInputMode="adjustPan"
            android:exported="true"/>

        <!-- SAMP Game Activity -->
        <activity
            android:name=".game.SAMP"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar.FullScreen"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|uiMode"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustPan"
            android:exported="true" />

    </application>

</manifest>
